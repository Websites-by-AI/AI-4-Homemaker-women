# 🤖 راهنمای راه‌اندازی مربی هوشمند (RAG) — دیجی‌آموزش

این صفحه قدم‌به‌قدم توضیح می‌دهد چطور «هوش مصنوعی شخصی‌سازی‌شده» فعال شود — بدون پایتون، روی همان Vercel + Neon.

## 🧩 معماری (در یک نگاه)

```
کاربر → /assistant (چت فارسی) → /api/ai/chat
          │
          ├─ پرسش → embedding (OpenRouter)
          ├─ جستجوی کسینوسی روی ai_chunks در Neon (PDF/یوتیوب/متن)
          └─ پرامپت فارسی + منابع → مدل چت (OpenRouter) → پاسخ شخصی‌سازی‌شده
```

- **بدون کلید هم سایت سالم می‌ماند** — مربی «حالت نمایشی» را با پیام فارسی نشان می‌دهد.
- embeddingها به‌صورت JSON در Neon ذخیره می‌شوند (بدون نیاز به pgvector؛ برای +۱۰هزار قطعه در آینده قابل مهاجرت است).

## ۱) گرفتن کلید رایگان OpenRouter

1. برو به `openrouter.ai` → Sign up (با گوگل)
2. منوی **Keys** → **Create Key**
3. کلید را کپی کن (شروع می‌شود با `sk-or-...`)
4. چند دلار اعتبار رایگان می‌گیری؛ مدل‌های `:free` هم در OpenRouter هست

## ۲) ست‌کردن متغیرها در Vercel

**Vercel → Project → Settings → Environment Variables** (هر سه محیط):

| نام | مقدار نمونه |
|---|---|
| `OPENROUTER_API_KEY` | `sk-or-v1-...` |
| `OPENROUTER_CHAT_MODEL` | `google/gemini-2.0-flash-001` (پیش‌فرض) |
| `OPENROUTER_EMBED_MODEL` | `openai/text-embedding-3-small` (پیش‌فرض) |

> می‌توانی هر مدلی از OpenRouter را بگذاری — مثلاً برای نسخهٔ کاملاً رایگان: `CHAT_MODEL=google/gemini-2.0-flash-exp:free`

سپس **Redeploy**.

## ۳) ساخت جداول دانش

بعد از دپلوی، یک‌بار seed را صدا بزن (جداول ai_documents و ai_chunks با drizzle-kit push در زمان بیلد ساخته می‌شوند — نیازی به کار اضافه نیست).

## ۴) افزودن PDF و ویدیو به دانش 🧠

با کاربر **admin** وارد شو (پیش‌فرض seed: `admin@teamcoder.com / 123456` — آن را عوض کن!) و برو به `/assistant`:

- **📕 PDF آموزشی** — درگ/انتخاب فایل (تا ۸ مگ) → خودکار چانک + embedding
- **🎬 ویدیوی یوتیوب** — لینک بده؛ ترنسکرایپت/زیرنویس خودکار خوانده می‌شود
- **📝 متن خام** — از مسیر API: `POST /api/ai/ingest` با `{type:"text", title, text}`

بعد از این هر پرسش کاربر با **منابع** جواب داده می‌شود و اسم منبع‌ها زیر پاسخ چیده می‌شود.

## ⚙️ نکات فنی

| مورد | توضیح |
|---|---|
| زمان اجرای APIها | `maxDuration = 60` ثانیه (مناسب پلن رایگان Vercel) |
| حافظهٔ گفت‌وگو | ۶ پیام آخر به مدل داده می‌شود |
| امنیت | چت فقط برای کاربران واردشده؛ افزودن/حذف سند فقط admin/manager |
| تست محلی | `OPENROUTER_API_KEY` در `.env.local` بگذار و `npm run dev` |
